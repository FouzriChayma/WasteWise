<form method="post" action="{{ path('app_store_house_delete', {'idSh': store_house.idSh}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ store_house.idSh) }}">
    <button class="btn">Delete</button>
</form>
